# Project Rules & Customizations for GiaPhaHoPham

## Automatic GitHub Sync Rule
- Whenever code, UI, database, or asset changes are completed in this repository, ALWAYS automatically prepare, run, or prompt the user to commit and push changes to GitHub (`git add .`, `git commit -m "..."`, `git push` or execute `sync.bat`).
- Maintain repository integrity and ensure GitHub remote always reflects the latest state after every task.
