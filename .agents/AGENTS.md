# Project Rules & Customizations for GiaPhaHoPham

## Automatic GitHub Sync Rule
- Whenever code, UI, database, or asset changes are completed in this repository, ALWAYS automatically prepare, run, or prompt the user with `.\sync.bat` (or `sync.bat`) to commit and push changes to GitHub.
- Always provide the `.\sync.bat` command in the final turn response for quick 1-click execution.
- Maintain repository integrity and ensure GitHub remote always reflects the latest state after every task.

